image: node:alpine

lint:
    stage: test
    script:
        - npm i --only=development
        - npx eslint .

pages:
    stage: deploy
    script:
        - npm i --only=development
        - npx typedoc
    artifacts:
        paths:
            - public
    only:
        - v1.1.0 
